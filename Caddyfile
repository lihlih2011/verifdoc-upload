{
    debug
}

:80 {
    log {
        output stdout
    }

    # IMPORTANT: /api/* must be checked BEFORE /*
    handle /api/* {
        reverse_proxy backend:8000
    }

    handle /* {
        reverse_proxy frontend:80
    }
}
